
   DatTools
   ÍÍÍÍÍÍÍÍ

   Unzip the archive MD5-DOC.ZIP. It contains DatTools's HTML
   manual (16 pages).

   NOTE: Due to Window's incompetent API, the former language
         selection via menu had to be moved to a dialog. When
         you select any language via a radio button, the dia-
         log is closed and DatTools menu is set to the chosen
         language. In future versions, the "more" button will
         switch to additional languages (if present).



   DatTools
   ÍÍÍÍÍÍÍÍ

   Entpacken Sie MD5-DOC.ZIP. Das Archiv enth„lt das HTML Ma-
   nual der DatTools (16 Seiten).

   ACHTUNG: Dank Window's inkompetenter API musste die bisher
            per Men vorgenommene Sprachauswahl in einen Dia-
            log ausgelagert werden. Mit einem der Radiokn”pfe
            die Sprache ausw„hlen, um DatTools auf die ausge-
            w„hlte Sprache umzuschalten. Zuknftige Versionen
            werden mit dem "mehr"-Knopf zus„tzliche Sprachpa-
            kete anbieten (soweit vorhanden).
